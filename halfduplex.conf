[bot]
server = localhost
port = 64738
username = HalfDuplexBot
password = 
channel = Half-Duplex Channel
# For multiple channels, use comma-separated list:
# channels = Channel1,Channel2,Channel3

# Timing configuration (in seconds)
speak_delay = 0.2
restore_delay = 0.5

# Optional client certificate
# certfile = /path act/to/bot.pem

# Debug logging
debug = true
